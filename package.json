{
  "name": "package-json-updated-action",
  "version": "1.0.1",
  "description": "A GitHub Action which indicates when the package.json version has updated in the most recent commit",
  "main": "./dist/index.js",
  "type": "module",
  "scripts": {
    "build:tsc": "tsc",
    "build:ncc": "ncc build ./dist/index.js -o dist_ncc",
    "build": "npm run build:tsc && npm run build:ncc"
  },
  "keywords": [
    "GitHub-Action",
    "package.json",
    "npm",
    "versioning"
  ],
  "author": "Monty Dawson <hello@montydawson.co.uk>",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "@vercel/ncc": "^0.44.1",
    "typescript": "^6.0.3"
  }
}
